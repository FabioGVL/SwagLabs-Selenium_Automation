name: Seu Fluxo de Trabalho

on:
  push:
    branches:
      - master  # Adapte para o nome da sua branch principal

jobs:
  test:
    runs-on: ubuntu-latest  # Você pode ajustar o sistema operacional conforme necessário

    steps:
    - name: Checkout do Código
      uses: actions/checkout@v2

    - name: Configurar Chrome
      uses: actions/setup-chrome@v2

    - name: Configurar Chromedriver
      run: |
        echo "Configurando Chromedriver"
        echo "Chromedriver Path: C:\\Windows\\JavaIDE\\chromedriver.exe"
        dir C:\\Windows\\JavaIDE  # Lista os arquivos no diretório para fins de verificação

    - name: Ajustar Permissões do Chromedriver
      run: |
        # Adicione este passo apenas se necessário
        # chmod +x C:\\Windows\\JavaIDE\\chromedriver.exe

    - name: Executar Testes
      run: |
        # Comandos para executar seus testes aqui
        # Exemplo: mvn test -Dtest=NomeDoTeste

    - name: Finalizar
      run: echo "Testes concluídos"